#include<stdio.h>
main()
{
	int i,r,n,sum=0,temp;
	scanf("%d", &n);
	temp=n;
	while(n>0)
	{
		r=n%10;
		sum=sum+(r*r*r);
		n=n/10;
	}
	if(temp==sum)
	printf("Armstrong number");
	else
	printf("Not a Armstrong number");
}
